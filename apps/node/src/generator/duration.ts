// parseDuration moved to @hammr/shared so the browser dashboard can
// validate scenario durations client-side. This file is a re-export for
// back-compat with existing imports inside the generator + scenario engine.
export { parseDuration } from '@hammr/shared';
